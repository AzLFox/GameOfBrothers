# <Фича> — <короткая формулировка цели>

**Ветка:** `<feature-name>` (от `develop`, merge в `develop`)
**Цель:** <одна фраза: что получаем в итоге>
<!-- при необходимости: **Хранилище (v1):** … / **Стек:** … / **Приоритеты:** … -->

Каждая фаза — отдельная сессия работы. Выполнять по порядку.

## Принципы

- <инвариант, который нельзя нарушать ни в одной фазе>
- <разделение builtin/пользовательских данных, лимиты домена, префиксы id — если применимо>
- Не переписывать на React/Vue; не трогать игровые формулы листа.
- Desktop не ломать; `prefers-reduced-motion` учитывать (для визуальных фич).

<!-- (Опционально) Модель данных / параметры — таблица полей, пример JSON, ключи хранилища, заглушки.
     Удалить секцию, если фича не вводит новых сущностей/ассетов. -->

---

## Фаза <X1> — <краткое название>

**Промпт:**
> 1. <конкретный шаг: файл `server/…` или `public/js/…`, функция, маршрут `/api/…`>
> 2. <следующий шаг>
> 3. <…>

**Критерии готовности:**
- [ ] <объективно проверяемый результат>
- [ ] <…>

---

## Фаза <X2> — <краткое название>

**Промпт:**
> 1. <…>
> 2. <…>

**Критерии готовности:**
- [ ] <…>
- [ ] <…>

<!-- Копируй блок «## Фаза …» столько раз, сколько нужно. Необязательные фазы
     помечай в заголовке «(опционально)». По готовности: заголовок → «(✅ готово)»,
     чекбоксы → [x], при желании добавь «**Реализовано:**  <что и где сделано>». -->

---

## Порядок

```
X1 → X2 → X3 → (X4)
```

<!-- Одной фразой: какие фазы дают MVP, какие можно отложить. -->

## Быстрые правила для всех фаз

- Ветка: `<feature-name>` → merge в `develop` после закрытия <минимального набора фаз>.
- Коммиты: `feat(<scope>): <фаза/суть>` / `fix(<scope>): …`
- Не добавлять React/Vue; не трогать игровые формулы листа.
- Каждая фаза — маленький коммит; после фазы — smoke-проверка (`node server/server.js` → http://localhost:3000).

<!-- (Опционально) ## Бюджеты — таблица «Метрика | Цель» для измеримых фич. -->
<!-- (Опционально) ## Smoke-сценарии (приёмка) — пронумерованные end-to-end проверки + регрессия. -->

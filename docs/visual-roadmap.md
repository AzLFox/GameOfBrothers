# GoB Visual Roadmap — промпты для реализации

Цель: удобный и красивый UI для настольной DnD-сессии (не игра-движок).
Приоритеты: **кинематографичный UI** + **живая главная сцена**.
Стек: HTML/CSS/JS + GSAP (+ Pixi.js на главной, фаза 4).

Каждый промпт — отдельная сессия работы. Выполнять по порядку.

---

## Фаза 1 — Фундамент motion (✅ готово)

**Промпт:**
> В проекте GoB (ветка `project_rethinking`) заложи фундамент для кинематографичного UI:
> 1. Подключи GSAP через npm и `/vendor/gsap`.
> 2. Создай `public/css/gob-tokens.css` — цвета, типографика, длительности, easing-пресеты.
> 3. Создай `public/js/gob-motion.js` — общий API: `prefers-reduced-motion`, easing, `timeline()`, хелперы `fadeUp`, `staggerIn`, `pulseGlow`.
> 4. На главной (`index.html` / `index.js`) замени ручные `requestAnimationFrame`-анимации карусели и флипа карточки на GSAP.
> 5. Добавь GSAP intro-timeline: завесы → горы → дерево → заголовок → карточки (stagger).
> Не трогай лист персонажа. Сохрани текущий визуальный стиль, улучши плавность и синхронность.

**Критерии готовности:**
- [ ] GSAP доступен на всех страницах через общий путь
- [ ] `gob-motion.js` переиспользуется без дублирования easing
- [ ] Открытие/закрытие карточки на главной — GSAP timeline
- [ ] Intro воспроизводится один раз, уважает `prefers-reduced-motion`

---

## Фаза 2 — Кинематографичный UI: главная страница (✅ готово)

**Промпт:**
> Доведи главную страницу GoB до кинематографичного уровня на GSAP:
> 1. Закрытие карточки — обратный timeline (не мгновенный сброс): spotlight fade, flip back, carousel expand, overlay out.
> 2. Hover/focus на карточках в орбите — лёгкий `translateZ` + glow без ломания `rear`-логики.
> 3. Кнопка «Новый персонаж» — micro-interaction (shine sweep, scale).
> 4. Переход по «Открыть» — page-out: затемнение + scale карточки → navigate (или fade всей сцены).
> 5. Синхронизировать `card-select.mp3` с пиком анимации (не с началом).
> Сохрани 3D-карусель и центрирование. Не добавляй Pixi пока.

**Критерии готовности:**
- [ ] Close-card так же красив, как open-card
- [ ] Нет рывков при смене `rear` во время анимации
- [ ] Переход на `/character` ощущается как «сцена из игры»

---

## Фаза 3 — Кинематографичный UI: лист персонажа (✅ готово)

**Промпт:**
> Подключи `gob-tokens.css` и `gob-motion.js` к `character.html`. Сделай кинематографичный UI листа персонажа:
> 1. Page-enter: портрет → статы → бой → экипировка → рюкзак (stagger 80–120ms).
> 2. Спеллбук: перелистывание страниц — GSAP timeline (угол страницы, тень, звук опционально).
> 3. Модалки/редактор слотов — slide+fade слева, не резкое появление.
> 4. Combat tooltips — fade+slide, без скачков позиции.
> 5. Числовые поля HP/AP/MP — мягкий flash при изменении (цвет 300ms).
> Не менять игровую логику и формулы. Только motion-слой поверх существующего JS.

**Критерии готовности:**
- [ ] Открытие спеллбука ощущается как книга, не как dialog
- [ ] Редактор слота не «прыгает»
- [ ] Все анимации отключаются при `prefers-reduced-motion`

---

## Фаза 4 — Живая главная сцена (Pixi.js) (✅ готово)

**Промпт:**
> Добавь на главную GoB живую атмосферу через Pixi.js (только `index.html`, canvas под `.scene`):
> 1. Fullscreen canvas `pointer-events: none`, z-index между `realm-depth` и каруселью.
> 2. Частицы: мотыльки/искры вокруг `world-tree` (teal + gold, low count для perf).
> 3. Мягкое пульсирующее свечение у корней дерева (radial gradient sprite или filter).
> 4. Реакция на выбранную карточку: чуть больше частиц + теплее свечение.
> 5. `prefers-reduced-motion` → статичный лёгкий glow без частиц.
> Карусель и клики не ломать. Pixi не трогать character page.

**Критерии готовности:**
- [ ] 60fps на средней машине, < 200 частиц
- [ ] Canvas resize на window resize
- [ ] Дерево и карточки визуально «в одном мире»

---

## Фаза 5 — Звук, полировка, общие переходы (✅ готово)

**Промпт:**
> Финальная полировка motion в GoB:
> 1. Единый звуковой слой: hover, open, close, page flip (громкость + throttle).
> 2. Общий page-transition helper в `gob-motion.js` для index ↔ character ↔ create.
> 3. Loading state при fetch `/api/characters` — skeleton/shimmer карточек вместо пустоты.
> 4. Performance pass: `will-change` только на время анимации, kill tweens on navigate.
> 5. Пройтись по mobile (640px): intro короче, меньше частиц.

**Критерии готовности:**
- [x] `gob-sound.js` — процедурный Web Audio, throttle, unlock на первый клик
- [x] `GobMotion.navigateTo()` на главной, листе и create
- [x] Skeleton/shimmer карусели во время fetch
- [x] `will-change` на карусели и флипе карточки
- [x] Mobile: короче intro, меньше частиц в Pixi

---

## Фаза 6 — Арт-направление (✅ готово)

**Промпт:**
> Усиль визуальную целостность GoB без смены стека:
> 1. Единый набор рамок (9-slice PNG) для панелей листа и карточек.
> 2. Фоновая текстура пергамента для `card-content` и панелей статов.
> 3. Иконки спелов — без изменения существующих (только добавление новых при необходимости).
> 4. Проверить контраст текста (WCAG AA для основного текста).
> Референс: HoMM3 UI + BG3 character sheet, но читаемость для сессии за столом.

**Критерии готовности:**
- [x] SVG 9-slice рамки (`frame-panel.svg`, `frame-card.svg`) + `gob-art.css`
- [x] Пергаментная текстура на панелях листа и `card-content`
- [x] Контраст текста: `--gob-text-body` / `--gob-text-muted` (WCAG AA на пергаменте)
- [x] Иконки спелов без изменений — используются оригинальные `.png` из `character.css`

---

## Фаза 6b — Реалистичный спеллбук (StPageFlip) (✅ готово)

**Промпт:**
> Замени плоский CSS/GSAP flip спеллбука на библиотеку `page-flip` (StPageFlip):
> 1. `npm install page-flip`, vendor route `/vendor/page-flip`.
> 2. `spellbook-flip.mjs` + динамическая сборка HTML-страниц из сетки заклинаний.
> 3. Внутренний `#spellbook-flip-root` (destroy не должен удалять внешний mount).
> 4. Инициализация только при открытом `dialog`, layout после анимации открытия.
> 5. Звук page-flip через `GobSound`, навигация prev/next через `flipPrev` / `flipNext`.
> Иконки спелов не менять.

**Критерии готовности:**
- [x] Мягкий curl листа (clip-path), тени StPageFlip
- [x] Повторное открытие книги не ломает DOM
- [x] Страницы видны с первого открытия

---

## Фаза 7 — Сквозной арт-дирекшн («один фильм») (✅ готово)

**Референс:** [Ivress — SPIN A TALE](https://brand.ivress.co.jp/) — единая палитра, атмосфера, типографика как сцена.

**Промпт:**
> Сделай визуальную целостность GoB на всех страницах (ветка `project_rethinking`):
> 1. Расширь `gob-tokens.css`: color grade (teal-тени, gold-свет), display/body/counter типографика.
> 2. В `gob-art.css` — глобальный cinematic pass: лёгкий vignette + film grain (CSS/SVG, 3–5% opacity).
> 3. Примени 9-slice рамки (`frame-panel.svg`) ко всем панелям листа, не только stats.
> 4. Карточки персонажей: золотая кромка + inner glow в одной системе с листом.
> 5. Спеллбук: оправа и корешок в той же палитре, что панели листа.
> 6. Орнаменты: общий набор SVG-углов/рун для панелей и модалок.
> Иконки спелов не менять. Читаемость за столом — приоритет.

**Критерии готовности:**
- [ ] Все страницы ощущаются одним миром (index / character / create)
- [ ] Display-заголовки с letter-spacing, body-текст без потери контраста
- [ ] Grain + vignette не мешают UI и отключаются при `prefers-reduced-motion`

---

## Фаза 8 — Порог в мир (ритуал входа) (частично)

> Overlay «Войти в мир» отложен. Сейчас: mist-loading, page-wipe transition.

**Промпт:**
> Добавь на главную «ритуал входа» в духе brand-site (CLICK TO START), без блокировки повторных визитов:
> 1. Overlay «Войти в мир» — один раз за сессию (sessionStorage) или всегда с быстрым skip.
> 2. По клику: `GobSound.unlock()` + запуск `playMainIntro`.
> 3. `prefers-reduced-motion` → сразу в интерфейс без overlay.
> 4. Loading персонажей: силуэты/туман вместо голого skeleton (Pixi или CSS), подпись «Имена проступают из тумана…».
> 5. Унифицировать page-transition: fullscreen wipe (teal → gold) через `GobMotion.navigateTo` на всех страницах.
> Не ломать карусель и быстрый доступ к листу при повторном визите.

**Критерии готовности:**
- [ ] Первый визит ощущается как «вход в мир»
- [ ] Повторный визит — без лишних кликов
- [ ] Переход index ↔ character ↔ create — один стиль

---

## Фаза 9 — Живая сцена 2.0 (главная)

**Промпт:**
> Подними главную сцену GoB до уровня «живой главы» (референс: атмосфера Ivress):
> 1. Pixi: parallax тумана/гор от движения мыши (лёгкий, ≤8px смещение).
> 2. God-rays / пульс свечения у корней `world-tree`, усиление при выбранной карточке.
> 3. Карусель: золотая дуга-орбита (SVG/CSS), spotlight на активной карте с falloff.
> 4. Idle «дыхание» сцены: tree scale 1.0 ↔ 1.02, 5–6s yoyo.
> 5. Ротация `page-hint` — 4–5 поэтичных подсказок через GSAP fade.
> 6. Ambient loop (wind + chimes), mute-кнопка, усиление при open-card / navigate.
> Budget: ≤120 частиц desktop, ≤60 mobile. `prefers-reduced-motion` → glow only.

**Критерии готовности:**
- [ ] Сцена реагирует на мышь и выбор карточки
- [ ] 60fps на средней машине
- [ ] Ambient не autoplay без жеста пользователя

---

## Фаза 10 — Лист и книга как артефакты

**Промпт:**
> Доведи character sheet и спеллбук до ощущения физического реквизита (BG3 + настольная сессия):
> 1. Page-enter листа: портрет → имя → зоны stagger ~100ms, лёгкий parallax панелей при скролле.
> 2. Combat tooltips: пергаментный хвостик, не плоский прямоугольник.
> 3. HP/AP/MP: flash + краткий рунический импульс по краю поля.
> 4. Спеллбук: анимация открытия обложки (scale + leather sound), номера страниц «— 03 —».
> 5. Табы специализаций: активный tab — gold glow; смена таба — flash корешка.
> 6. Редактор заклинаний: slide-in как лист из книги; preview иконки в кольце слота.
> StPageFlip не заменять. Иконки спелов не менять.

**Критерии готовности:**
- [ ] Лист читаем за столом, но ощущается «собранным вручную»
- [ ] Спеллбук — книга, не dialog
- [ ] Редактор заклинаний не выбивается из книги

---

## Фаза 11 — Cinematic UI pass (финальная полировка)

**Промпт:**
> Финальный polish pass по всему GoB:
> 1. Кастомный cursor glow на интерактивах главной (desktop).
> 2. Focus states: золотое кольцо вместо browser default.
> 3. Scrollbar (desktop): тонкий, пергаментный.
> 4. Favicon + `<title>` в стиле бренда.
> 5. Mobile pass: короче intro, spellbook portrait при <640px (тест StPageFlip).
> 6. Perf audit: `will-change` только на время анимации, destroy Pixi/StPageFlip on navigate.
> 7. Прогон WCAG AA на пергаменте и тёмных панелях.
> Только polish — без новых фич листа.

**Критерии готовности:**
- [ ] Нет визуальных «дыр» между страницами
- [ ] Mobile и reduced-motion проверены
- [ ] Lighthouse accessibility ≥90 на character page

---

## Фаза 12 — Опционально: лор-страница (нарратив)

**Промпт:**
> Отдельная лёгкая страница `/lore` или модал на главной — 5–7 «глав» истории мира:
> 1. GSAP ScrollTrigger, тот же визуальный язык что index.
> 2. Не блокирует доступ к инструментам сессии.
> 3. Прогресс «01 / 07» как у brand-site.
> Выполнять только если нужен нарративный слой продукта.

**Критерии готовности:**
- [ ] Стилистически часть GoB, не отдельный сайт
- [ ] Можно пропустить и сразу играть

---

## Быстрые правила для всех фаз

- Ветка: `project_rethinking`
- Не переписывать на React/Vue
- Логика DnD-листа в приоритете над эффектами
- Каждая фаза — маленький коммит с понятным сообщением
- После фазы — проверить главную + лист в браузере

# TO-DEL.md — кандидаты на удаление

> **Назначение.** Список файлов, которые выглядят мусорными/бесполезными и предлагаются к удалению.
> **Содержимое.** Путь файла + причина. Файлы здесь **пока не удалены** — это список на подтверждение.
> После согласования их можно удалить (`git rm`) и вычистить из истории при необходимости.
> Каталог всех рабочих документов — [INDEX.md](INDEX.md).

---

## Текущие кандидаты

Пусто — на данный момент кандидатов на удаление нет.

_Удалено ранее:_ 19 корневых PNG-скриншотов разработки (`backs-90.png`, `tree-*`, `carousel-*`,
`with-tree.png` и т. д.) и осиротевший корневой `index.css`; `docs/INTEGRATION.md` (см. ниже).

## GameOfBraza заморожен

Сестринский проект **GameOfBraza** больше не поддерживается — заморожен, все разработчики
перешли на GameOfBrothers. Поэтому все упоминания GameOfBraza убраны из проекта:

- `docs/INTEGRATION.md` (спецификация обмена персонажами с GameOfBraza) **удалён** — вся его суть
  сводилась к интеграции с мёртвым проектом. Восстановить при необходимости можно из истории git.
- Упоминания вычищены из [../CLAUDE.md](../CLAUDE.md) и [INDEX.md](INDEX.md).

Сама фича экспорта/импорта листа **сохранена** — теперь это самодостаточный JSON-снимок листа
(`schemaVersion` 2, модалка «Перенос» в `public/js/character.js`), не привязанный к GameOfBraza.

## Намеренно оставлено (не кандидаты)

- `public/img/ui/dove-letter.svg` — формально не подключён (UI использует `dove-letter.png`), но
  оставлен осознанно: SVG удобен как масштабируемый шаблон при дальнейшем развитии, в отличие от PNG.
  Не помечать как мусор.

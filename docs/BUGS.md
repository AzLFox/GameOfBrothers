# BUGS.md — реестр багов

> **Назначение.** Единый список подтверждённых дефектов GameOfBrothers — того, что
> сейчас работает неправильно.
> **Содержимое.** Каждый пункт: суть бага, следствия, файлы/строки, план фикса.
> Не путать с [TO-DO.md](TO-DO.md) (идеи развития) и [TO-DEL.md](TO-DEL.md) (файлы на удаление).
> Каталог всех рабочих документов — [INDEX.md](INDEX.md).

---

## B1. Нет взаимного исключения: два мастера могут вести одну группу одновременно

Замысел: любой ГМ может взять любую свободную группу, но **не одновременно** с другим —
блокировкой служит `gmUserId` + проверка `canGmAccessGroup`.

Фактически блокировка не сохраняется: `normalizePartyGroup()` не переносит `gmUserId`/`gmAssignedAt`
в возвращаемый объект, а `savePartyGroup()` пишет объект целиком (`writeJson`, без мерджа), поэтому
`PUT /api/gm/session`, делающий `normalizePartyGroup({ ...group, gmUserId })`, сохраняет группу
**без** `gmUserId`. Подтверждено на рантайм-данных: в `server/data/parties/grp_*.json` ключа
`gmUserId` нет вообще.

Следствия:
- `canGmAccessGroup()` всегда возвращает `true` (условие `!group.gmUserId`), ошибка
  «Группа ведётся другим мастером» недостижима — ограничение «не одновременно» не работает.
- В сводке групп (`gmGroupSummary`) `gmUserId` пуст, `isActiveGm` всегда `false`.

Файлы: [../server/server.js:301](../server/server.js#L301) (`normalizePartyGroup`),
[../server/server.js:445](../server/server.js#L445) (`canGmAccessGroup`),
[../server/server.js:811](../server/server.js#L811) (`PUT /api/gm/session`),
[../server/store.js:132](../server/store.js#L132) (`savePartyGroup`).

Фикс: сохранять `gmUserId`/`gmAssignedAt` в `normalizePartyGroup` (или писать их в обход
нормализации) — **обязательно вместе с механизмом освобождения группы** (см. [TO-DO.md](TO-DO.md),
«освобождение группы / lease»), иначе группа навсегда залипнет за первым открывшим её мастером.

---

## B2. Аккаунт ГМ отображается как аккаунт игрока при создании группы (✅ исправлено)

При создании группы аккаунт мастера показывался как обычный игрок — роль GameMaster в этом
интерфейсе не учитывалась. Причина: `GET /api/group/accounts` отдавал аккаунты **без `role`**, а
`accountCard()` в [../public/js/group.js](../public/js/group.js) подписывал все чужие карточки
«Аккаунт игрока» — ветки для ГМ не было.

**Исправлено:** сервер добавляет `role: auth.resolveUserRole(u)` в каждый аккаунт
([../server/server.js:605](../server/server.js#L605)); `accountCard()`
([../public/js/group.js:390](../public/js/group.js#L390)) подписывает мастерский аккаунт
«Аккаунт мастера» и вешает классы-модификаторы `--gm` (стили — [../public/css/group.css](../public/css/group.css)).
Покрыто тестом `/api/group/accounts отдаёт role` в [../tests/server.test.js](../tests/server.test.js).
Роадмап — [b2-gm-account-label-roadmap.md](b2-gm-account-label-roadmap.md).

Скрытие ГМ из списка **приглашаемых** аккаунтов в фикс не входит — это отдельная тема «явный
элемент ГМ в модели группы» (см. [TO-DO.md](TO-DO.md)).

---

## B3. `PUT /api/me/sheets/:charId` не валидирует тело

Сервер принимает произвольный JSON и пишет его как есть — серверной нормализации листа нет
(`defaultSheetForCard` используется только при создании). Теперь, когда в чужой лист может писать и
мастер (см. [TO-DO.md](TO-DO.md), «мастер правит листы героев» — сделано), отсутствие валидации
превращается из мелочи в риск целостности данных и безопасности.

Фикс: добавить `normalizeSheet()` по аналогии с `normalizePartyGroup` и прогонять тело через него
перед записью.

---

## B4. Щиты дают пассивную защиту вместо активной проверки (✅ исправлено)

Замысел: щит **не** должен давать пассивную защиту. Его значение применяется только при удачной
проверке в момент получения урона; два щита (в обеих руках) = две отдельные проверки.

Было: класс `shield` ([../public/js/item-catalog.js:89](../public/js/item-catalog.js#L89)) давал
модификатор `armor` (`type: 'flat'`), который через `sumEquipmentMods`
([../public/js/character.js:709](../public/js/character.js#L709)) постоянно вливался в боевую строку
«Защита» — то есть щит работал как пассивная броня. Тот же ключ `armor` у латных наручей
`bracers_plate` — настоящая пассивная броня, её отделять было нельзя.

**Исправлено:** щиту выделен собственный модификатор `shieldGuard` (label «Щит»,
[../public/js/item-catalog.js:36](../public/js/item-catalog.js#L36)); класс `shield` отдаёт
`shieldGuard` вместо `armor`, поэтому агрегат «Защита» его больше не видит. Щит **не
агрегируется**: каждый надетый щит выводится собственной боевой строкой с подписью по руке —
«Щит в левой руке» / «Щит в правой руке» (`renderShieldRows`,
[../public/js/character.js](../public/js/character.js)) — как урон у оружия: щитов может быть два,
каждый = отдельная строка и отдельная проверка. Латные наручи/классовая броня END остаются на
`armor` без изменений. Добавлена миграция сохранённых листов `migrateShieldItem`
(`armor`→`shieldGuard` у щитов) во всех точках входа предметов: снаряжение (`migrateEquipment`),
рюкзак (`migrateBackpack`) и импорт JSON. Роадмап —
[b4-shield-active-guard-roadmap.md](b4-shield-active-guard-roadmap.md).

Активный расчёт входящего урона (кнопка «получить пизды» — С2·3, шаг «Щит в руке») остаётся
отдельной задачей ([TO-DO.md](TO-DO.md)); он будет читать значения щитов через
`equipmentModSources('shieldGuard')`.

---

## B5. Двуручное оружие затирает предмет в другой руке

При установке двуручного оружия в редактируемую руку `reconcileHands`
([../public/js/character.js:3627](../public/js/character.js#L3627)) пишет зеркало во вторую руку
(`sheet.equipment[other] = mirror`, [:3635](../public/js/character.js#L3635)) — предмет, который там
был, молча удаляется.

Нужно **запрещать** экипировку двуручного оружия, если другая (не редактируемая в данный момент)
рука занята: не давать выбрать двуручный класс / показывать предупреждение, пока рука не
освобождена, вместо тихого удаления.

---

## B6. На телефоне перенос из снаряжения в рюкзак требует одновременного зажатия и тапа

Чтобы переложить вещь снаряжение → рюкзак на телефоне, сейчас нужно **одновременно** держать вещь и
тапнуть по слоту рюкзака. Нужен последовательный флоу: зажатие/тап **выделяет** вещь, следующий тап
по слоту — **переносит** (без одновременного удержания и нажатия).
